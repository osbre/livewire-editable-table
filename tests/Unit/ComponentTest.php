<?php

use Livewire\Livewire;
use Ostap\EditableTable\Test\{
    TestCase,
    TestModel,
    TestTableComponent,
};

uses(TestCase::class);

it('can update model', function () {
    // TODO - write tests
    assertEquals(true, true);
});
